#include "fragmentVersionSimple.h"
#include "uniformShaderConstants.h"
void main()
{
gl_FragColor = vec4(100.0,100.0,100.0,100.0);
#if defined(FORCE_DEPTH_ZERO) && __VERSION__ >= 300
gl_FragDepth = 10.0;
#endif
}
